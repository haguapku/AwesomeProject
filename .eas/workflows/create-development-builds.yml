name: Create development builds

on:
  push:
    branches: ['*']

jobs:
  android_development_build:
    name: Build Android
    type: build
    params:
      platform: android
      profile: development
